From ada32f2d2dc42d3cb69c48286ba8cb30c34ec888 Mon Sep 17 00:00:00 2001
From: tracer-ysyx <tracer@ysyx.org>
Date: Tue, 26 May 2026 20:45:15 +0800
Subject: [PATCH 0863/1487] > run NEMU ysyx_22040000 xyzabc Linux xyzabc
 6.8.0-117-generic #117~22.04.1-Ubuntu SMP PREEMPT_DYNAMIC Thu May  7 22:17:46
 UTC  x86_64 x86_64 x86_64 GNU/Linux  20:45:15 up  1:01,  1 user,  load
 average: 0.23, 0.27, 0.18

---
 abstract-machine/scripts/platform/nemu.mk | 2 ++
 nemu/src/monitor/monitor.c                | 6 +++++-
 2 files changed, 7 insertions(+), 1 deletion(-)

diff --git a/abstract-machine/scripts/platform/nemu.mk b/abstract-machine/scripts/platform/nemu.mk
index 9ada6b2..81c2d48 100644
--- a/abstract-machine/scripts/platform/nemu.mk
+++ b/abstract-machine/scripts/platform/nemu.mk
@@ -14,6 +14,8 @@ LDFLAGS   += --defsym=_pmem_start=0x80000000 --defsym=_entry_offset=0x0
 LDFLAGS   += --gc-sections -e _start
 ##打开 -b 选项来打开批处理模式
 NEMUFLAGS += -l $(shell dirname $(IMAGE).elf)/nemu-log.txt -b
+##传入elf文件
+NEMUFLAGS += -e $(IMAGE).elf
 
 MAINARGS_MAX_LEN = 64
 MAINARGS_PLACEHOLDER = the_insert-arg_rule_in_Makefile_will_insert_mainargs_here
diff --git a/nemu/src/monitor/monitor.c b/nemu/src/monitor/monitor.c
index 089c647..be5fd36 100644
--- a/nemu/src/monitor/monitor.c
+++ b/nemu/src/monitor/monitor.c
@@ -76,15 +76,18 @@ static int parse_args(int argc, char *argv[]) {
     {"diff"     , required_argument, NULL, 'd'},
     {"port"     , required_argument, NULL, 'p'},
     {"help"     , no_argument      , NULL, 'h'},
+    //添加elf
+    {"elf"      , required_argument, NULL, 'e'},
     {0          , 0                , NULL,  0 },
   };
   int o;
-  while ( (o = getopt_long(argc, argv, "-bhl:d:p:", table, NULL)) != -1) {
+  while ( (o = getopt_long(argc, argv, "-bhl:d:p:e:", table, NULL)) != -1) {
     switch (o) {
       case 'b': sdb_set_batch_mode(); break;
       case 'p': sscanf(optarg, "%d", &difftest_port); break;
       case 'l': log_file = optarg; break;
       case 'd': diff_so_file = optarg; break;
+      case 'e': img_file = optarg; break;
       case 1: img_file = optarg; return 0;
       default:
         printf("Usage: %s [OPTION...] IMAGE [args]\n\n", argv[0]);
@@ -92,6 +95,7 @@ static int parse_args(int argc, char *argv[]) {
         printf("\t-l,--log=FILE           output log to FILE\n");
         printf("\t-d,--diff=REF_SO        run DiffTest with reference REF_SO\n");
         printf("\t-p,--port=PORT          run DiffTest with port PORT\n");
+        printf("\t-e,--elf=ELF            run with ELF file to trace function\n");
         printf("\n");
         exit(0);
     }
-- 
2.34.1

