From 918872265d7fb46bb117d8470af6615da34cbd78 Mon Sep 17 00:00:00 2001
From: tracer-ysyx <tracer@ysyx.org>
Date: Wed, 22 Apr 2026 20:58:40 +0800
Subject: [PATCH 0079/1487] > compile NEMU ysyx_22040000 xyzabc Linux xyzabc
 6.8.0-110-generic #110~22.04.1-Ubuntu SMP PREEMPT_DYNAMIC Fri Mar 27 12:43:08
 UTC  x86_64 x86_64 x86_64 GNU/Linux  20:58:40 up 26 min,  1 user,  load
 average: 0.28, 0.22, 0.16

---
 nemu/src/monitor/sdb/expr.c | 41 +++++++++++++++++++++++++++++++------
 nemu/src/monitor/sdb/sdb.c  |  4 ++++
 2 files changed, 39 insertions(+), 6 deletions(-)

diff --git a/nemu/src/monitor/sdb/expr.c b/nemu/src/monitor/sdb/expr.c
index 6a2993b..654c40b 100644
--- a/nemu/src/monitor/sdb/expr.c
+++ b/nemu/src/monitor/sdb/expr.c
@@ -21,10 +21,11 @@
 #include <regex.h>
 
 enum {
-  TK_NOTYPE = 256, TK_EQ,
-
-  /* TODO: Add more token types */
-
+  TK_NOTYPE = 256, 
+  TK_EQ, 
+  TK_HEX, 
+  TK_DEC, 
+  TK_REG,
 };
 
 static struct rule {
@@ -37,8 +38,16 @@ static struct rule {
    */
 
   {" +", TK_NOTYPE},    // spaces
+  {"\\(", '('},         // left parentheses
+  {"\\)", ')'},         // right parentheses
+  {"\\*", '*'},         // multiply
+  {"/", '/'},            // divide
   {"\\+", '+'},         // plus
+  {"-", '-'},            // minus
   {"==", TK_EQ},        // equal
+  {"0[xX][0-9a-fA-F]+", TK_HEX},  // hexadecimal number
+  {"[0-9]+", TK_DEC},             // decimal number
+  {"\\$[a-zA-Z]+", TK_REG}        // register
 };
 
 #define NR_REGEX ARRLEN(rules)
@@ -94,8 +103,28 @@ static bool make_token(char *e) {
          * of tokens, some extra actions should be performed.
          */
 
-        switch (rules[i].token_type) {
-          default: TODO();
+        // switch (rules[i].token_type) {
+        //   //General operations
+        //   default: {
+        //     memset(tokens[nr_token].str, 0, sizeof(tokens[nr_token].str));// Clear the string before copying
+        //     strncpy(tokens[nr_token].str, substr_start, substr_len);// Copy the matched substring to the token's str field
+        //     tokens[nr_token].type = rules[i].token_type;
+        //     nr_token++;
+        //     break;
+        //   }
+        //   case TK_NOTYPE: {
+        //     // Do nothing for spaces
+        //     break;
+        //   }
+        //   case 
+        // }
+
+        //temp test
+        if(rules[i].token_type != TK_NOTYPE) {
+          memset(tokens[nr_token].str, 0, sizeof(tokens[nr_token].str));// Clear the string before copying
+          strncpy(tokens[nr_token].str, substr_start, substr_len);// Copy the matched substring to the token's str field
+          tokens[nr_token].type = rules[i].token_type;
+          nr_token++;
         }
 
         break;
diff --git a/nemu/src/monitor/sdb/sdb.c b/nemu/src/monitor/sdb/sdb.c
index 9523ec3..c6c0359 100644
--- a/nemu/src/monitor/sdb/sdb.c
+++ b/nemu/src/monitor/sdb/sdb.c
@@ -58,6 +58,10 @@ static int cmd_si(char *args) {
   if (args != NULL) {
     n = atoi(args);
   }
+  if(n <= 0) {
+    printf("Invalid number of steps: %d\n", n);
+    return 0;
+  }
   cpu_exec(n);
   return 0;
 }
-- 
2.34.1

