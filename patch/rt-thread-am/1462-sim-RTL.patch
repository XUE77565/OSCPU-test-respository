From 2ebd395753c8ec093d75f5eb48422e54899ff84f Mon Sep 17 00:00:00 2001
From: tracer-ysyx <tracer@ysyx.org>
Date: Mon, 8 Jun 2026 17:02:11 +0800
Subject: [PATCH 1462/1487] > sim RTL ysyx_22040000 xyzabc Linux xyzabc
 6.8.0-124-generic #124~22.04.1-Ubuntu SMP PREEMPT_DYNAMIC Tue May 26 21:05:19
 UTC  x86_64 x86_64 x86_64 GNU/Linux  17:02:11 up  1:28,  1 user,  load
 average: 0.26, 0.26, 0.34

---
 npc/Makefile | 8 ++++++--
 1 file changed, 6 insertions(+), 2 deletions(-)

diff --git a/npc/Makefile b/npc/Makefile
index 1fcf4a8..3743a1a 100644
--- a/npc/Makefile
+++ b/npc/Makefile
@@ -55,17 +55,21 @@ $(TARGET): $(VSRCS) $(CSRCS)
 	@mkdir -p $(BUILD_DIR)
 	verilator $(VERILATOR_FLAGS) $(VSRCS) $(CSRCS) -o $(TARGET)
 
+# 仿真模式: 默认 batch, 传 MODE=-i 切换到交互模式
+# 用法: make ARCH=minirv-npc ALL=add run MODE=-i
+MODE ?=
+
 # sim 目标：先通过 git_commit 记录 RTL 变更，再编译运行
 # IMG 变量由外部传入，指定要加载的二进制文件路径
 sim:
 	$(call git_commit, "sim RTL")  # DO NOT REMOVE THIS LINE!!!
 	@$(MAKE) all
 	@echo "Running simulation with image: $(IMG)"
-	$(TARGET) $(IMG)
+	$(TARGET) $(IMG) $(MODE)
 
 # run 目标：直接运行已编译的仿真（不重新编译）
 run:
-	$(TARGET) $(IMG)
+	$(TARGET) $(IMG) $(MODE)
 
 # clean 目标：清理所有构建产物
 clean:
-- 
2.34.1

