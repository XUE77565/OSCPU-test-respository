From 9948439520f3158896fe86f9b84218f68ff5f5f9 Mon Sep 17 00:00:00 2001
From: tracer-ysyx <tracer@ysyx.org>
Date: Thu, 4 Jun 2026 15:50:09 +0800
Subject: [PATCH 1178/1487] > run NEMU ysyx_22040000 xyzabc Linux xyzabc
 6.8.0-124-generic #124~22.04.1-Ubuntu SMP PREEMPT_DYNAMIC Tue May 26 21:05:19
 UTC  x86_64 x86_64 x86_64 GNU/Linux  15:50:09 up 46 min,  1 user,  load
 average: 0.17, 0.22, 0.31

---
 nemu/src/device/io/map.c | 2 ++
 1 file changed, 2 insertions(+)

diff --git a/nemu/src/device/io/map.c b/nemu/src/device/io/map.c
index 6239b5a..8b85809 100644
--- a/nemu/src/device/io/map.c
+++ b/nemu/src/device/io/map.c
@@ -54,6 +54,7 @@ void init_map() {
 
 word_t map_read(paddr_t addr, int len, IOMap *map) {
   assert(len >= 1 && len <= 8);
+  printf("map_read: addr = 0x%08x, len = %d\n", addr, len);
   check_bound(map, addr);
   paddr_t offset = addr - map->low;
   invoke_callback(map->callback, offset, len, false); // prepare data to read
@@ -63,6 +64,7 @@ word_t map_read(paddr_t addr, int len, IOMap *map) {
 
 void map_write(paddr_t addr, int len, word_t data, IOMap *map) {
   assert(len >= 1 && len <= 8);
+  printf("map_write: addr = 0x%08x, len = %d, data = 0x%08x\n", addr, len, data);
   check_bound(map, addr);
   paddr_t offset = addr - map->low;
   host_write(map->space + offset, len, data);
-- 
2.34.1

