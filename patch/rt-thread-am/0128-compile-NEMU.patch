From 0132861ee3cecf2624f3a63c28ec0d649f78b160 Mon Sep 17 00:00:00 2001
From: tracer-ysyx <tracer@ysyx.org>
Date: Wed, 13 May 2026 19:27:02 +0800
Subject: [PATCH 0128/1487] > compile NEMU ysyx_22040000 xyzabc Linux xyzabc
 6.8.0-111-generic #111~22.04.1-Ubuntu SMP PREEMPT_DYNAMIC Tue Apr 14 17:13:45
 UTC  x86_64 x86_64 x86_64 GNU/Linux  19:27:02 up  2:34,  1 user,  load
 average: 0.50, 0.40, 0.31

---
 nemu/src/isa/riscv32/inst.c       | 15 +++++++++++----
 nemu/src/monitor/sdb/expr.c       |  8 --------
 nemu/src/monitor/sdb/watchpoint.c |  3 ---
 3 files changed, 11 insertions(+), 15 deletions(-)

diff --git a/nemu/src/isa/riscv32/inst.c b/nemu/src/isa/riscv32/inst.c
index 5ef8331..e1aeb04 100644
--- a/nemu/src/isa/riscv32/inst.c
+++ b/nemu/src/isa/riscv32/inst.c
@@ -62,14 +62,21 @@ static int decode_exec(Decode *s) {
 }
 
   INSTPAT_START();
+  //TYPE-U
   INSTPAT("??????? ????? ????? ??? ????? 00101 11", auipc  , U, R(rd) = s->pc + imm);
+
+  //TYPE-I
   INSTPAT("??????? ????? ????? 100 ????? 00000 11", lbu    , I, R(rd) = Mr(src1 + imm, 1));
-  INSTPAT("??????? ????? ????? 000 ????? 01000 11", sb     , S, Mw(src1 + imm, 1, src2));
   INSTPAT("??????? ????? ????? 000 ????? 00100 11", addi   , I, R(rd) = src1 + imm);//li是伪指令，解析为addi x0
-  INSTPAT("??????? ????? ????? ??? ????? 11011 11", jal    , J, R(rd) = s->snpc; s->dnpc = s->pc + imm);
-  INSTPAT("??????? ????? ????? 010 ????? 01000 11", sw     , S, Mw(src1 + imm, 4, src2));
-  //ret被展开为jalr
   INSTPAT("??????? ????? ????? 000 ????? 11001 11", jalr   , I, R(rd) = s->snpc; s->dnpc = src1 + imm);
+
+  //TYPE-S
+  INSTPAT("??????? ????? ????? 010 ????? 01000 11", sw     , S, Mw(src1 + imm, 4, src2));
+  INSTPAT("??????? ????? ????? 000 ????? 01000 11", sb     , S, Mw(src1 + imm, 1, src2));
+
+  //TYPE-J
+  INSTPAT("??????? ????? ????? ??? ????? 11011 11", jal    , J, R(rd) = s->snpc; s->dnpc = s->pc + imm);
+  
   INSTPAT("0000000 00001 00000 000 00000 11100 11", ebreak , N, NEMUTRAP(s->pc, R(10))); // R(10) is $a0
   INSTPAT("??????? ????? ????? ??? ????? ????? ??", inv    , N, INV(s->pc));
   INSTPAT_END();
diff --git a/nemu/src/monitor/sdb/expr.c b/nemu/src/monitor/sdb/expr.c
index 0942d17..379a58e 100644
--- a/nemu/src/monitor/sdb/expr.c
+++ b/nemu/src/monitor/sdb/expr.c
@@ -37,10 +37,6 @@ static struct rule {
   int token_type;
 } rules[] = {
 
-  /* TODO: Add more rules.
-   * Pay attention to the precedence level of different rules.
-   */
-
   {" +", TK_NOTYPE},    // spaces
   {"\\(", '('},         // left parentheses
   {"\\)", ')'},         // right parentheses
@@ -118,10 +114,6 @@ static bool make_token(char *e) {
 
         position += substr_len;
 
-        /* TODO: Now a new token is recognized with rules[i]. Add codes
-         * to record the token in the array `tokens'. For certain types
-         * of tokens, some extra actions should be performed.
-         */
 
         //temp test
         if(rules[i].token_type != TK_NOTYPE) {
diff --git a/nemu/src/monitor/sdb/watchpoint.c b/nemu/src/monitor/sdb/watchpoint.c
index 9004165..6df95c6 100644
--- a/nemu/src/monitor/sdb/watchpoint.c
+++ b/nemu/src/monitor/sdb/watchpoint.c
@@ -114,6 +114,3 @@ void wp_display() {
   }
 }  
 
-
-/* TODO: Implement the functionality of watchpoint */
-
-- 
2.34.1

