From f1dd0da5ecf30f717ab47bb00c097115f1e1c424 Mon Sep 17 00:00:00 2001
From: tracer-ysyx <tracer@ysyx.org>
Date: Thu, 23 Apr 2026 18:30:02 +0800
Subject: [PATCH 0099/1487] > compile NEMU ysyx_22040000 xyzabc Linux xyzabc
 6.8.0-110-generic #110~22.04.1-Ubuntu SMP PREEMPT_DYNAMIC Fri Mar 27 12:43:08
 UTC  x86_64 x86_64 x86_64 GNU/Linux  18:30:02 up  3:35,  1 user,  load
 average: 0.48, 0.46, 0.36

---
 nemu/src/monitor/sdb/expreval.c | 20 ++++++++++----------
 1 file changed, 10 insertions(+), 10 deletions(-)

diff --git a/nemu/src/monitor/sdb/expreval.c b/nemu/src/monitor/sdb/expreval.c
index fbd640c..45f6a91 100644
--- a/nemu/src/monitor/sdb/expreval.c
+++ b/nemu/src/monitor/sdb/expreval.c
@@ -53,17 +53,17 @@ int expreval(int p, int q, Token *tokens, bool *success) {
             return 0;
         }
         val1 = expreval(p, op - 1, tokens, success);
-        printf("Strings of tokens from %d to %d: ", p, op - 1);
-        for(int i = p; i < op; i++) {
-            printf("%s ", tokens[i].str);
-        }
-        printf("\n[%d, %d]val1 = %u\n", p, op - 1, val1);
+        // printf("Strings of tokens from %d to %d: ", p, op - 1);
+        // for(int i = p; i < op; i++) {
+        //     printf("%s ", tokens[i].str);
+        // }
+        // printf("\n[%d, %d]val1 = %u\n", p, op - 1, val1);
         val2 = expreval(op + 1, q, tokens, success);
-        printf("Strings of tokens from %d to %d: ", op + 1, q);
-        for(int i = op + 1; i <= q; i++) {
-            printf("%s ", tokens[i].str);
-        }
-        printf("\n[%d, %d]val2 = %u\n", op + 1, q, val2);
+        // printf("Strings of tokens from %d to %d: ", op + 1, q);
+        // for(int i = op + 1; i <= q; i++) {
+        //     printf("%s ", tokens[i].str);
+        // }
+        // printf("\n[%d, %d]val2 = %u\n", op + 1, q, val2);
 
         switch (tokens[op].type) {
             case '+': return val1 + val2;
-- 
2.34.1

