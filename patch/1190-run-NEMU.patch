From 899f6c96dbf6a087cca9346fff535e5a6f73d9d4 Mon Sep 17 00:00:00 2001
From: tracer-ysyx <tracer@ysyx.org>
Date: Thu, 4 Jun 2026 16:06:59 +0800
Subject: [PATCH 1190/1487] > run NEMU ysyx_22040000 xyzabc Linux xyzabc
 6.8.0-124-generic #124~22.04.1-Ubuntu SMP PREEMPT_DYNAMIC Tue May 26 21:05:19
 UTC  x86_64 x86_64 x86_64 GNU/Linux  16:06:59 up  1:02,  1 user,  load
 average: 0.35, 0.42, 0.35

---
 abstract-machine/klib/src/stdio.c | 155 +++++++++++++++++++++++-------
 1 file changed, 121 insertions(+), 34 deletions(-)

diff --git a/abstract-machine/klib/src/stdio.c b/abstract-machine/klib/src/stdio.c
index f22ec18..f78068c 100644
--- a/abstract-machine/klib/src/stdio.c
+++ b/abstract-machine/klib/src/stdio.c
@@ -5,71 +5,156 @@
 
 #if !defined(__ISA_NATIVE__) || defined(__NATIVE_USE_KLIB__)
 
+// 输出一个字符到缓冲区
+#define PUTC(ch) do { \
+  if (n > 0 && count < n - 1) out[count] = (ch); \
+  count++; \
+} while(0)
+
 int vsnprintf(char *out, size_t n, const char *fmt, va_list ap) {
   size_t count = 0;
+
   while (*fmt != '\0') {
     if (*fmt == '%') {
       fmt++;
+
+      // 解析标志: '-' 左对齐, '0' 补零
+      bool left_align = false;
+      bool pad_zero = false;
+      while (*fmt == '-' || *fmt == '0') {
+        if (*fmt == '-') left_align = true;
+        if (*fmt == '0') pad_zero = true;
+        fmt++;
+      }
+
+      // 解析宽度
+      int width = 0;
+      while (*fmt >= '0' && *fmt <= '9') {
+        width = width * 10 + (*fmt - '0');
+        fmt++;
+      }
+
+      // 跳过长度修饰符 (l, ll 等, RV32下与默认相同)
+      while (*fmt == 'l' || *fmt == 'h') fmt++;
+
       if (*fmt == 'd') {
         int num = va_arg(ap, int);
         unsigned int unum;
+        bool neg = false;
         if (num < 0) {
-          if (n > 0 && count < n - 1) {
-            out[count] = '-';
-          }
-          count++;
-          //这样避免了溢出的循环
+          neg = true;
           unum = (unsigned int)(-(num + 1)) + 1;
         } else {
           unum = (unsigned int)num;
         }
+
+        // 计算数字位数
+        char buf[20];
+        int i = 0;
+        do {
+          buf[i++] = unum % 10 + '0';
+          unum /= 10;
+        } while (unum > 0);
+
+        int num_len = i + (neg ? 1 : 0);
+        int pad_len = (width > num_len) ? (width - num_len) : 0;
+
+        // 零填充时, 负号在前: -0003
+        if (neg && pad_zero) PUTC('-');
+        // 左侧填充
+        if (!left_align) {
+          char pc = pad_zero ? '0' : ' ';
+          for (int j = 0; j < pad_len; j++) PUTC(pc);
+        }
+        // 空格填充时, 负号在填充后:    -3
+        if (neg && !pad_zero) PUTC('-');
+        // 输出数字
+        for (int j = i - 1; j >= 0; j--) PUTC(buf[j]);
+        // 右侧填充 (左对齐)
+        if (left_align) {
+          for (int j = 0; j < pad_len; j++) PUTC(' ');
+        }
+
+      } else if (*fmt == 'u') {
+        unsigned int unum = va_arg(ap, unsigned int);
         char buf[20];
         int i = 0;
         do {
           buf[i++] = unum % 10 + '0';
           unum /= 10;
         } while (unum > 0);
-        for (int j = i - 1; j >= 0; j--) {
-          if (n > 0 && count < n - 1) {
-            out[count] = buf[j];
-          }
-          count++;
+
+        int pad_len = (width > i) ? (width - i) : 0;
+        if (!left_align) {
+          char pc = pad_zero ? '0' : ' ';
+          for (int j = 0; j < pad_len; j++) PUTC(pc);
+        }
+        for (int j = i - 1; j >= 0; j--) PUTC(buf[j]);
+        if (left_align) {
+          for (int j = 0; j < pad_len; j++) PUTC(' ');
+        }
+
+      } else if (*fmt == 'x' || *fmt == 'X') {
+        unsigned int unum = va_arg(ap, unsigned int);
+        int upper = (*fmt == 'X');
+        char buf[20];
+        int i = 0;
+        do {
+          buf[i++] = upper ? "0123456789ABCDEF"[unum % 16] : "0123456789abcdef"[unum % 16];
+          unum /= 16;
+        } while (unum > 0);
+
+        int pad_len = (width > i) ? (width - i) : 0;
+        if (!left_align) {
+          char pc = pad_zero ? '0' : ' ';
+          for (int j = 0; j < pad_len; j++) PUTC(pc);
+        }
+        for (int j = i - 1; j >= 0; j--) PUTC(buf[j]);
+        if (left_align) {
+          for (int j = 0; j < pad_len; j++) PUTC(' ');
+        }
+
+      } else if (*fmt == 'c') {
+        char ch = (char)va_arg(ap, int);
+        int pad_len = (width > 1) ? (width - 1) : 0;
+        if (!left_align) {
+          for (int j = 0; j < pad_len; j++) PUTC(' ');
+        }
+        PUTC(ch);
+        if (left_align) {
+          for (int j = 0; j < pad_len; j++) PUTC(' ');
         }
+
       } else if (*fmt == 's') {
         char *str = va_arg(ap, char *);
         if (str == NULL) str = "(null)";
-        while (*str != '\0') {
-          if (n > 0 && count < n - 1) {
-            out[count] = *str;
-          }
-          count++;
+        int slen = 0;
+        while (str[slen]) slen++;
+        int pad_len = (width > slen) ? (width - slen) : 0;
+        if (!left_align) {
+          for (int j = 0; j < pad_len; j++) PUTC(' ');
+        }
+        while (*str) {
+          PUTC(*str);
           str++;
         }
-      } else if (*fmt == '%') {
-        if (n > 0 && count < n - 1) {
-          out[count] = '%';
+        if (left_align) {
+          for (int j = 0; j < pad_len; j++) PUTC(' ');
         }
-        count++;
+
+      } else if (*fmt == '%') {
+        PUTC('%');
       } else {
-        // 对于尚未支持的格式（比如 %x），原样输出
-        if (n > 0 && count < n - 1) {
-          out[count] = '%';
-        }
-        count++;
-        if (n > 0 && count < n - 1) {
-          out[count] = *fmt;
-        }
-        count++;
+        // 不支持的格式原样输出
+        PUTC('%');
+        if (*fmt) PUTC(*fmt);
       }
     } else {
-      if (n > 0 && count < n - 1) {
-        out[count] = *fmt;
-      }
-      count++;
+      PUTC(*fmt);
     }
     fmt++;
   }
-  
+
   if (n > 0) {
     if (count < n) {
       out[count] = '\0';
@@ -80,6 +165,8 @@ int vsnprintf(char *out, size_t n, const char *fmt, va_list ap) {
   return (int)count;
 }
 
+#undef PUTC
+
 int snprintf(char *out, size_t n, const char *fmt, ...) {
   va_list ap;
   va_start(ap, fmt);
@@ -107,7 +194,7 @@ int printf(const char *fmt, ...) {
   va_start(ap, fmt);
   int ret = vsnprintf(buf, sizeof(buf), fmt, ap);
   va_end(ap);
-  
+
   for (int i = 0; i < ret && i < sizeof(buf) - 1; i++) {
     putch(buf[i]);
   }
-- 
2.34.1

