From 406bd729f33221e40f906c29a3c958659069b2df Mon Sep 17 00:00:00 2001
From: tracer-ysyx <tracer@ysyx.org>
Date: Mon, 1 Jun 2026 17:56:39 +0800
Subject: [PATCH 1053/1487] > run NEMU ysyx_22040000 xyzabc Linux xyzabc
 6.8.0-117-generic #117~22.04.1-Ubuntu SMP PREEMPT_DYNAMIC Thu May  7 22:17:46
 UTC  x86_64 x86_64 x86_64 GNU/Linux  17:56:39 up  2:43,  1 user,  load
 average: 0.36, 0.28, 0.22

---
 abstract-machine/am/src/platform/nemu/ioe/gpu.c | 10 ++++++++++
 1 file changed, 10 insertions(+)

diff --git a/abstract-machine/am/src/platform/nemu/ioe/gpu.c b/abstract-machine/am/src/platform/nemu/ioe/gpu.c
index 4ed5ca4..0aeff5a 100644
--- a/abstract-machine/am/src/platform/nemu/ioe/gpu.c
+++ b/abstract-machine/am/src/platform/nemu/ioe/gpu.c
@@ -29,6 +29,16 @@ void __am_gpu_config(AM_GPU_CONFIG_T *cfg) {
 }
 
 void __am_gpu_fbdraw(AM_GPU_FBDRAW_T *ctl) {
+  uint32_t screen_wh = inl(VGACTL_ADDR);
+  uint32_t *fbctl = (uint32_t *)FB_ADDR;
+  uint32_t *pixels = (uint32_t *)ctl->pixels;
+  int x = ctl->x, y = ctl->y, w = ctl->w, h = ctl->h;
+  for (int j = 0; j < h; j++) {
+    for (int i = 0; i < w; i++) {
+      fbctl[(y + j) * (screen_wh >> 16) + (x + i)] = pixels[j * w + i];
+    }
+  }
+
   if (ctl->sync) {
     outl(SYNC_ADDR, 1);
   }
-- 
2.34.1

